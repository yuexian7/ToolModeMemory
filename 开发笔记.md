# Tool Mode Memory 开发笔记

> 模组名：**Tool Mode Memory**（工具模式记忆）  
> 版本：v0.1.0（本地开发，不上传平台）  
> 工作区：`D:\WorkSpace\CS2MOD\ToolModeMemory`  
> 零 Harmony：只读公开事件 + 轮询捕获 + 公开属性写回。

## 步骤 1 · 机制调研结论（反编译证据）

### 1.1 原版记忆矩阵

| 状态项 | 原版是否记忆 | 粒度 | 证据 |
|---|---|---|---|
| NetTool Mode/Snap/Parallel/Underground | ✅ | `UIObjectData.m_Group` | `NetToolSystem.cs` L47-76, L5498-5534 |
| NetTool elevation | ❌ 不入字典 | 全局会话字段 | L4888-4892, `ResetToolPreferences` 归零 |
| Toolbar 分类/资产 | ✅ | 菜单 / 分类 | `ToolbarUISystem.cs` L86-88, L1267-1271 |
| Object/Zone/Bulldoze/Area/Water 模式 | ❌ | 无字典 | `mode { get; set; }` 裸属性 |
| 生命周期 | `OnGamePreload` 全清 | 不落盘 | `NetToolSystem.cs` L5545-5548 |

### 1.2 关键公开 API（零 Harmony 可用）

- `ToolSystem.EventToolChanged` / `EventPrefabChanged` / `activeTool` / `activePrefab`
- `NetToolSystem`：`mode` `selectedSnap` `parallelCount` `parallelOffset` `underground` `elevation` 公开属性
- `ObjectToolSystem` / `ZoneToolSystem` / `BulldozeToolSystem` / `AreaToolSystem` 等 `mode` 公开
- `GameManager.onGameSaveLoad(saveName, previewUri, start, success)`
- `GameManager.onGamePreload` / `onGameLoadingComplete` / `gameMode`
- `Application.quitting` + `AppDomain.ProcessExit` 做退出落盘（Playbook §3.1）

### 1.3 设置框架硬约束（AccessAnarchy / ZoneSnapper 踩坑）

1. **下拉框**必须 `[SettingsUIDropdown]` + `[SettingsUIValueVersion]`（语言切换要重取 items）。
2. **每份语言字典各填各的语言**（传入 locale，勿读 activeLocale）。
3. **按钮**是 **只写 bool** + `[SettingsUIButton]`；点击 = `SetValue(true)`。
4. **键位标签**用 `GetOptionLabelLocaleID(nameof(Prop))`，不是 `GetBindingKeyLocaleID`。
5. `LoadSettings` **先于** `RegisterKeyBindings`。
6. 系统类必须 `partial`；`ILog.Warn` 不是 `Warning`。
7. 运行时数据写 `ModsData\ToolModeMemory\`，**禁止**写 `Mods\<name>\`（DeployWIP 会删）。

## 12 官方语言

`de-DE en-US es-ES fr-FR it-IT ja-JP ko-KR pl-PL pt-BR ru-RU zh-HANS zh-HANT`

## 产品设计（v0.1.0）

### 记忆文件

- 路径：`%USERPROFILE%\AppData\LocalLow\Colossal Order\Cities Skylines II\ModsData\ToolModeMemory\<存档名>.json`
- 每存档 **1 份**，退出时覆盖写入
- 新档未命名：`_unsaved_<session>.json`，首次存档后改名为存档名并删旧文件

### 范围（Scope）

| 值 | 名称 | 键 |
|---|---|---|
| 0 | 同组 | `G:{UIObjectData.m_Group}` |
| 1 | 同菜单 | `M:{assetMenu}` |
| 2 | 同类资产 | `C:{UIAssetCategoryData}` |
| 3 | 全局共用 | `S` |
| 4 | 全局不共用 | `P:{prefabIndex}` |

每项下拉选项标注：`（推荐原版）` / `（原版）` / `（推荐）` / 无标注。

### 工具项目录（设置共用范围）

| id | 面板行 | 资产类型 | 原版 | 推荐 |
|---|---|---|---|---|
| net.draw | 绘制模式 | 道路/轨道/管道 | 同组 | 同组 |
| net.snap | 吸附 | 同上 | 同组 | 同组 |
| net.parallel | 平行道路 | 道路等 | 同组 | 同组 |
| net.underground | 地下/高架 | 隧道/高架 | 同组 | 同组 |
| net.elevation | 高程 | 道路/轨道 | 全局共用 | 同组 |
| obj.place | 放置模式 | 建筑/摆件/树 | 无记忆 | 同类资产 |
| obj.align | 对齐/吸附 | 建筑/摆件/树 | 无记忆 | 同组 |
| obj.underground | 地下放置 | 管道/地下建筑 | 无记忆 | 同组 |
| zone.mode | 区域绘制 | 市辖区/产业区 | 无记忆 | 同组 |
| area.mode | 表面区域 | 垃圾填埋等 | 无记忆 | 同组 |
| water.mode | 水体工具 | 水源 | 无记忆 | 同组 |
| terrain.mode | 地形工具 | 地形笔刷 | 无记忆 | 全局共用 |
| bulldoze.mode | 推土范围 | 全部 | 无记忆 | 全局共用 |
| upgrade.mode | 升级/重建 | 道路升级等 | 无记忆 | 同类资产 |

总开关默认 **关**（= 原版）。每项默认启用推荐范围。

### 设置页

- 标签页：`ModSettings`（模组设置）/ `MemoryManage`（记忆管理）
- 模组设置：总开关 + 「设置共用范围」14 组（启用 + 范围下拉）
- 记忆管理：重置记忆（确认框）+ 打开记忆文件夹

### 捕获 / 恢复策略（零 Harmony）

1. `EventToolChanged` / `EventPrefabChanged` 后 **下一帧** 按范围键写回（盖过原版 Group 字典）。
2. 工具激活期间 **每 30 帧** 轮询捕获到内存库（键=当前范围解析）。
3. `onGameLoadingComplete(Game)` → 读文件 → 恢复。
4. `onGameSaveLoad(end)` / 回主菜单 / `Application.quitting` → 覆盖写盘。
5. 总关 / 单项关：不捕获不写回，行为 = 原版。

### 性能 / 安全

- 关闭时系统 `Enabled=false`，零分配
- 不修改游戏文件、零 Harmony、不 `UnpatchAll`
- OnDispose 只 Unregister + 落盘，不碰其它模组
- 退出 IO 超时收紧（文件小，同步写 <1ms）

## v0.1.1 修复（实机「高度仍共用」）

**根因**：`NetToolSystem.CheckElevationRange` 执行  
`elevation = Clamp(m_DesiredElevation, range)`。  
只写公开 `elevation` 会在换 prefab 时被私有 `m_DesiredElevation`（仍是会话共用值）冲掉。

**修复**：
1. `ApplyElevation` 反射同步写 `m_Elevation` + `m_DesiredElevation`，并重置 `m_LastElevationRange`。
2. 捕获改为**每帧**（不再 30 帧轮询）。
3. prefab 变化**当帧** Apply；未命中时：GlobalUnique / elevation **写默认值**，防止状态串资产。
4. 「记忆管理」页改名「关于」：版本 yuexian / 三个外链按钮。
5. 中文用词对齐官方 Locale.cok：高度（非高程）、装饰（非摆件）、推土机、功能区、直线/简单曲线/复合曲线/连续/替换、填充/滚动/刷涂、表面区域规划工具。

## 未验证 / 实机待补

- `UIObjectData` 字段布局（编译期以游戏 DLL 为准）
- 菜单实体解析链（Category → Menu）
- 云存档 displayName 与本地文件名一致性

## 发布

**本轮禁止上传 Paradox / 上传 GitHub。** 仅本地 `dotnet build` + DeployWIP。
